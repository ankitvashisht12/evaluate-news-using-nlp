import { updateUI } from "../client/js/updateUI.js";

describe("Testing updateUI function", () => {
  test("should have updateUI function defined", () => {
    expect(updateUI).toBeDefined();
  });
});
